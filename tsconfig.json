{
    "compilerOptions": {
      "outDir": "./dist"
      // Add other compiler options as needed
    },
    "include": ["src/**/*.ts"]
    // Adjust the "include" pattern to match your project's TypeScript files
  }